import React from 'react';
import { useLoaderData } from 'react-router-dom';

const SpotsOfCountry = () => {
    // const loadedData = useLoaderData()
    console.log('loadedData');
    return (
        <div>
            country er data paben
        </div>
    );
};

export default SpotsOfCountry;